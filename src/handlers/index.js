const homeHandler = require("./homeHandler");
const publicHandler = require("./publicHandler");
const createPostHandler = require("./createPost");
const postsHandler = require("./postsHandler");

module.exports = {
    homeHandler,
    publicHandler,
    createPostHandler,
    postsHandler,
};
